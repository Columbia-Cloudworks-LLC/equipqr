// Work order costs related constants
export const WORK_ORDER_COSTS_CONSTANTS = {
  MIN_QUANTITY: 1,
  MIN_PRICE: 0,
  DEFAULT_QUANTITY: 1,
  DEFAULT_PRICE: 0,
  CURRENCY_CENTS_MULTIPLIER: 100,
} as const;