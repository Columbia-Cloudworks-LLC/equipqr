# ─────────────────────────────────────────────────────────────────────────────
# EquipQR — .env.example
# Copy to .env for local development. Do NOT commit your filled .env.
# Client-readable variables MUST be prefixed with VITE_ (Vite exposes them).
# Server-only secrets live in Supabase Edge Function secrets, or in
# supabase/functions/.env ONLY for local 'supabase functions serve'.
# ─────────────────────────────────────────────────────────────────────────────

# ── Client (Vite) — safe to expose to the browser
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_STRIPE_PUBLISHABLE_KEY=
VITE_GOOGLE_MAPS_API_KEY=
# Optional: only if the client needs to build absolute URLs. Otherwise omit.
VITE_PRODUCTION_URL=

# ── Server / Edge Functions — DO NOT EXPOSE TO CLIENT
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_DB_URL=
RESEND_API_KEY=
HCAPTCHA_SECRET_KEY=
SUPABASE_URL=                 # server may need the base URL as well
SUPABASE_ANON_KEY=            # optional if anon ops are needed server-side
GOOGLE_MAPS_API_KEY=          # only if server-side geocoding/static maps
PRODUCTION_URL=               # canonical app URL for emails/links
